{
  "name": "cdc-precautions-viewer",
  "version": "0.1.0",
  "private": true,
  "description": "Search-first viewer for CDC Isolation Precautions Appendix A (Type and Duration of Precautions).",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build:data": "node scripts/build-data.mjs",
    "dev": "npm run build:data && vite",
    "build": "npm run build:data && vue-tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "fuse.js": "^7.0.0",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0",
    "tailwindcss": "^3.4.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vite-plugin-pwa": "^0.20.0",
    "vue-tsc": "^2.0.0"
  }
}
